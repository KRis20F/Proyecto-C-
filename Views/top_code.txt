TOP RANK:

for (var i = 0; i < 10; i++)
{
    string username = rankObject.data.users[i].username;
    string country = rankObject.data.users[i].country;
    double rating = rankObject.data.users[i].league.rating;

    Console.WriteLine($"{i+1} | {username} - {country} -- {rating}");
}

TOP SPRINT: 

for (var i = 0; i < 10; i++)
{
    int id = rankObject[i].id;
    string username = rankObject[i].name;

    Console.WriteLine($"{i + 1} | {username} - {id}");
}